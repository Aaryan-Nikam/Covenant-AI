from setuptools import setup, find_packages

setup(
    name="ironpass",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.11",
    description="Ironpass Python SDK — compliance proxy client",
)
